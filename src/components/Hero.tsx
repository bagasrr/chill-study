import { Typography } from "@mui/material";

export default function Hero() {
  return (
    <>
      <Typography variant="h1">Hellow</Typography>
      <section className={`font-poppins`}>
        <h1 className="text-4xl">Ini judul pake Poppins doang di sini</h1>
      </section>

      <section className={`font-pinyon`}>
        <h1 className="text-4xl">Ini judul pake Pinyon Script doang di sini</h1>
      </section>

      <section className={`font-roboto`}>
        <h1 className="text-4xl">Ini judul pake roboto doang di sini</h1>
      </section>

      <section className={`font-geist`}>
        <h1 className="text-4xl">Ini judul pake geist doang di sini</h1>
      </section>

      <section className={`font-mplus`}>
        <h1 className="text-4xl">Ini judul pake mplus doang di sini</h1>
      </section>

      <section className={`font-dmserif`}>
        <h1 className="text-4xl">Ini judul pake dmserif doang di sini</h1>
      </section>

      <section className={`font-patrickHand`}>
        <h1 className="text-4xl">Ini judul pake patrickHand doang di sini</h1>
      </section>

      <section className={`font-notoSans`}>
        <h1 className="text-4xl">Ini judul pake notoSans doang di sini</h1>
      </section>
    </>
  );
}
