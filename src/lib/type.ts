export type LeadershipCard = {
  image: string;
  name: string;
  role: string;
  responsibility: string;
};
export type PricingCardProps = {
  title: string;
  price: string;
  description: string;
};
